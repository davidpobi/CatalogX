# Security

Report vulnerabilities privately to the repository maintainers rather than opening a public issue. Never include credentials, private prompts, audio, personal data, or exploit payloads in a report.

CatalogX does not retain uploaded audio or raw search prompts. OpenAI calls use `store: false`. Provider credentials are server-only and public AI operations are rate-limited.
